(()=>{'use strict';
if(window.__adatacoreSecurityActivity)return;window.__adatacoreSecurityActivity=true;
if(!window.db)return;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=d=>{if(!d)return 'Unknown time';const x=new Date(d);return Number.isNaN(x.getTime())?'Unknown time':x.toLocaleString()};
function device(ua=''){ua=String(ua);let browser='Browser',os='Unknown device';if(/Edg\//i.test(ua))browser='Edge';else if(/Chrome\//i.test(ua)&&!/Chromium/i.test(ua))browser='Chrome';else if(/Firefox\//i.test(ua))browser='Firefox';else if(/Safari\//i.test(ua)&&!/Chrome\//i.test(ua))browser='Safari';if(/Windows/i.test(ua))os='Windows';else if(/Android/i.test(ua))os='Android';else if(/iPhone|iPad|iPod/i.test(ua))os='iOS / iPadOS';else if(/Macintosh|Mac OS X/i.test(ua))os='macOS';else if(/Linux/i.test(ua))os='Linux';return `${browser} · ${os}`}
const style=document.createElement('style');style.textContent=`.security-activity-card{margin-top:14px;padding:20px}.security-activity-head{display:flex;align-items:flex-start;justify-content:space-between;gap:14px}.security-activity-head h2{font-size:15px;margin:0}.security-activity-sub{font-size:10px;color:var(--muted);line-height:1.55;margin-top:5px}.security-activity-list{display:grid;gap:8px;margin-top:14px}.security-login{display:grid;grid-template-columns:1.05fr .95fr .72fr;gap:12px;padding:12px;border:1px solid var(--line);border-radius:12px;background:var(--panel2)}.security-login span{display:block;font-size:8px;color:var(--muted2);text-transform:uppercase;letter-spacing:.07em;font-weight:800}.security-login strong{display:block;margin-top:5px;font-size:10px;line-height:1.4;word-break:break-word}.security-ip{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.security-activity-note{font-size:9px;color:var(--muted);line-height:1.55;margin-top:12px;padding-top:11px;border-top:1px solid var(--line)}.security-empty{padding:18px 0;color:var(--muted);font-size:10px}@media(max-width:720px){.security-login{grid-template-columns:1fr}}`;document.head.appendChild(style);
const view=document.getElementById('view-profile');if(!view)return;
const card=document.createElement('section');card.id='accountSecurityActivity';card.className='card security-activity-card';card.innerHTML=`<div class="security-activity-head"><div><div class="eyebrow">Account security</div><h2>Recent sign-ins</h2><div class="security-activity-sub">Your recent authentication sessions. Network addresses are masked on your contributor profile.</div></div><button id="securityActivityRefresh" class="btn btn-secondary" type="button">Refresh</button></div><div id="securityActivityList" class="security-activity-list"><div class="security-empty">Loading recent sign-ins…</div></div><div class="security-activity-note">If you do not recognize a sign-in, change your password and contact Adatacore support. Shared Wi-Fi, mobile networks and VPNs can change the network address shown here.</div>`;
view.appendChild(card);
let loading=false,lastLoad=0;
async function load(force=false){if(loading||(!force&&Date.now()-lastLoad<15000))return;loading=true;const button=document.getElementById('securityActivityRefresh');if(button)button.disabled=true;try{const {data,error}=await db.rpc('my_security_activity');if(error)throw error;lastLoad=Date.now();const rows=Array.isArray(data?.sessions)?data.sessions:[];const list=document.getElementById('securityActivityList');list.innerHTML=rows.length?rows.map((r,i)=>`<div class="security-login"><div><span>${i===0?'Most recent':'Sign-in'}</span><strong>${esc(fmt(r.updated_at||r.created_at))}</strong></div><div><span>Device / browser</span><strong>${esc(device(r.user_agent))}</strong></div><div><span>Network</span><strong class="security-ip">${esc(r.ip_masked||'Unavailable')}</strong></div></div>`).join(''):'<div class="security-empty">No retained sign-in sessions are available yet.</div>'}catch(error){document.getElementById('securityActivityList').innerHTML=`<div class="security-empty">Unable to load recent sign-ins: ${esc(error.message||error)}</div>`}finally{loading=false;if(button)button.disabled=false}}
document.getElementById('securityActivityRefresh').onclick=()=>load(true);
window.addEventListener('hashchange',()=>{if(location.hash==='#profile')load()});
document.addEventListener('visibilitychange',()=>{if(!document.hidden&&location.hash==='#profile')load()});
if(location.hash==='#profile')load();else setTimeout(()=>load(),900);
})();