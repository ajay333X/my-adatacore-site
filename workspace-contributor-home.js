(()=>{'use strict';
const U='https://llmhyezgcnbognmmsnzq.supabase.co',K='sb_publishable_QfaSTpmmj6reyY-kCsmhng_7PKvCGml';
const db=supabase.createClient(U,K,{auth:{persistSession:true,autoRefreshToken:true}});const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let home=null;function fmtMoney(v){return '$'+Number(v||0).toFixed(2)}function when(v){if(!v)return '—';try{return new Date(v).toLocaleString()}catch(_){return '—'}}
function style(){if(document.getElementById('contributorHomeStyles'))return;const s=document.createElement('style');s.id='contributorHomeStyles';s.textContent=`.today-grid{display:grid;grid-template-columns:1.25fr .75fr;gap:14px}.today-stack{display:grid;gap:14px}.today-card{padding:20px}.today-card h3{margin:5px 0 8px;font-size:17px}.today-card p{margin:0;color:var(--muted);font-size:11px;line-height:1.6}.today-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.today-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:14px}.today-kpi{padding:16px}.today-kpi strong{display:block;font-size:23px;margin-top:5px}.today-kpi span{font-size:9px;color:var(--muted);text-transform:uppercase;letter-spacing:.07em}.timeline{display:grid;gap:9px}.timeline-row{display:grid;grid-template-columns:16px 1fr auto;gap:10px;align-items:start;padding:11px 0;border-bottom:1px solid var(--line)}.timeline-row:last-child{border-bottom:0}.tl-dot{width:9px;height:9px;border-radius:50%;margin-top:4px;background:var(--brand)}.tl-meta{font-size:10px;color:var(--muted);margin-top:3px}.notice-list{display:grid;gap:9px}.notice-item{border:1px solid var(--line);background:var(--panel2);border-radius:12px;padding:13px}.notice-item strong{font-size:12px}.notice-item p{font-size:10px;margin-top:5px}.notice-actions{margin-top:9px}.support-form{display:grid;gap:10px}.support-form input,.support-form textarea,.support-form select{width:100%;border:1px solid var(--line2);background:var(--panel2);color:var(--text);border-radius:10px;padding:10px;font:inherit;font-size:12px}.support-form textarea{min-height:110px;resize:vertical}.badge-count{display:inline-grid;place-items:center;min-width:18px;height:18px;border-radius:999px;background:var(--brand);color:#fff;font-size:9px;margin-left:auto;padding:0 5px}.today-empty{font-size:11px;color:var(--muted);padding:8px 0}@media(max-width:1000px){.today-grid{grid-template-columns:1fr}.today-kpis{grid-template-columns:repeat(2,1fr)}}@media(max-width:600px){.today-kpis{grid-template-columns:1fr}.today-actions{flex-direction:column}}`;document.head.appendChild(s)}
function addNav(){const group=document.querySelector('.nav-group');if(!group||group.querySelector('[data-view-link="today"]'))return;const label=group.querySelector('.nav-label');const a=document.createElement('a');a.className='nav-link';a.dataset.viewLink='today';a.href='#today';a.innerHTML='<span>Today</span><span id="todayUnreadBadge" class="badge-count" style="display:none">0</span>';label.after(a);const n=document.createElement('a');n.className='nav-link';n.dataset.viewLink='notifications';n.href='#notifications';n.textContent='Notifications';const sp=document.createElement('a');sp.className='nav-link';sp.dataset.viewLink='support';sp.href='#support';sp.textContent='Support';const prof=group.querySelector('[data-view-link="profile"]');if(prof){prof.before(n);prof.before(sp)}else{group.append(n,sp)}}
function addViews(){const main=document.querySelector('.app-main');if(!main||document.getElementById('view-today'))return;const views=`<section id="view-today" class="workspace-view"><div id="todayBody"><div class="card empty">Loading your day…</div></div></section><section id="view-notifications" class="workspace-view"><div class="section-title"><div><div class="eyebrow">Inbox</div><h2>Notifications</h2><div class="page-sub">Project updates, feedback, payment and account activity.</div></div><button id="markAllNotifications" class="btn btn-secondary" type="button">Mark all read</button></div><div id="notificationsBody" class="card today-card"><div class="today-empty">Loading notifications…</div></div></section><section id="view-support" class="workspace-view"><div class="today-grid"><div><div class="section-title"><div><div class="eyebrow">Help</div><h2>Support</h2><div class="page-sub">Report project, task, quality, payment, account or technical issues.</div></div></div><div class="card today-card"><form id="supportForm" class="support-form"><select id="supportCategory"><option value="project">Project</option><option value="task">Task</option><option value="quality">Quality / feedback</option><option value="payment">Payment</option><option value="account">Account</option><option value="technical">Technical</option><option value="other">Other</option></select><input id="supportSubject" maxlength="180" placeholder="Short subject" required><textarea id="supportDescription" maxlength="5000" placeholder="Describe what happened, what you expected, and any task/project details." required></textarea><button class="btn btn-primary" type="submit">Create support ticket</button><div id="supportStatus" class="notice"></div></form></div></div><div><div class="section-title"><div><div class="eyebrow">Your tickets</div><h2>Recent cases</h2></div></div><div id="supportTickets" class="card today-card"><div class="today-empty">Loading tickets…</div></div></div></div></section>`;main.insertAdjacentHTML('beforeend',views)}
function setupRouting(){const oldLinks=[...document.querySelectorAll('[data-view-link]')];oldLinks.forEach(a=>a.addEventListener('click',()=>setTimeout(route,0)));window.addEventListener('hashchange',route);route()}
function route(){const key=(location.hash||'#overview').slice(1);if(!['today','notifications','support'].includes(key))return;document.querySelectorAll('.workspace-view').forEach(v=>v.classList.toggle('active',v.id==='view-'+key));document.querySelectorAll('[data-view-link]').forEach(a=>a.classList.toggle('active',a.dataset.viewLink===key));const h=document.getElementById('viewHeading'),s=document.getElementById('viewSub'),e=document.getElementById('viewEyebrow');if(h)h.textContent=key==='today'?'Today':key[0].toUpperCase()+key.slice(1);if(e)e.textContent=key==='today'?'Contributor home':key==='notifications'?'Updates':'Contributor help';if(s)s.textContent=key==='today'?'What needs your attention right now.':key==='notifications'?'Your latest platform and project updates.':'Open and track support requests.';if(key==='notifications')loadNotifications();if(key==='support')loadSupport()}
function renderToday(){const x=home||{},p=x.performance||{},pay=x.payments||{},apps=x.applications||[],projects=x.projects||[],ann=x.announcements||[];const active=projects.filter(r=>['active','enabled','approved'].includes(String(r.access_status||'').toLowerCase()));const ready=projects.filter(r=>r.has_ready_work);const needsApp=apps.filter(a=>['draft','changes_requested'].includes(a.status));const nextPay=pay.next_scheduled_for?`Next scheduled ${when(pay.next_scheduled_for)}`:'No payout date scheduled yet';document.getElementById('todayBody').innerHTML=`<div class="today-kpis"><div class="card today-kpi"><span>Ready work</span><strong>${ready.length}</strong></div><div class="card today-kpi"><span>Unread updates</span><strong>${x.unread_notifications||0}</strong></div><div class="card today-kpi"><span>Approval rate</span><strong>${Number(p.approval_rate||0).toFixed(1)}%</strong></div><div class="card today-kpi"><span>Awaiting payout</span><strong>${fmtMoney(pay.awaiting)}</strong></div></div><div class="today-grid"><div class="today-stack"><div class="card today-card"><div class="eyebrow">Next action</div><h3>${ready.length?'You have work ready to start':needsApp.length?'Your application needs attention':active.length?'You are active — no task is waiting right now':'You are all caught up'}</h3><p>${ready.length?'Open Assignments and continue with your available work.':needsApp.length?esc(needsApp[0].next_step):active.length?'We will show new work here as soon as it becomes available.':'Check applications and project updates below for your next step.'}</p><div class="today-actions">${ready.length?'<a class="btn btn-primary" href="#assignments">Open assignments</a>':''}<a class="btn btn-secondary" href="#notifications">View notifications</a><a class="btn btn-secondary" href="#support">Get support</a></div></div><div class="card today-card"><div class="eyebrow">Project journey</div><h3>Your project status</h3><div class="timeline">${projects.length?projects.map(r=>`<div class="timeline-row"><span class="tl-dot"></span><div><strong>${esc(r.name)}</strong><div class="tl-meta">${esc(r.type||'Project')} · ${r.submission_count||0} submissions · ${r.approved_count||0} approved</div></div><span class="pill ${String(r.access_status).toLowerCase()==='active'?'pill-green':''}">${esc(r.access_status||r.lifecycle_status||'status')}</span></div>`).join(''):'<div class="today-empty">No project access has been assigned yet.</div>'}</div></div><div class="card today-card"><div class="eyebrow">Applications</div><h3>Assessment status</h3><div class="timeline">${apps.length?apps.map(a=>`<div class="timeline-row"><span class="tl-dot"></span><div><strong>${esc(a.language_label)} · ${esc(a.track==='voice_acting'?'Voice Acting':'Transcription')}</strong><div class="tl-meta">${esc(a.next_step)}${a.reviewer_note?` · Feedback: ${esc(a.reviewer_note)}`:''}</div></div><span class="pill">${esc(a.status)}</span></div>`).join(''):'<div class="today-empty">No applications yet.</div>'}</div></div></div><div class="today-stack"><div class="card today-card"><div class="eyebrow">Performance</div><h3>Your contributor snapshot</h3><p>${p.total_submissions||0} total submissions · ${p.approved||0} approved · ${p.rejected||0} rejected</p><div class="today-actions"><span class="pill pill-green">${Number(p.approval_rate||0).toFixed(1)}% approval</span><span class="pill">${fmtMoney(p.approved_earnings)} approved earnings</span></div></div><div class="card today-card"><div class="eyebrow">Payments</div><h3>${fmtMoney(pay.paid)} paid</h3><p>${nextPay}. Processing: ${fmtMoney(pay.processing)}.</p><div class="today-actions"><a class="btn btn-secondary" href="#earnings">Open earnings</a></div></div><div class="card today-card"><div class="eyebrow">Announcements</div><h3>Project updates</h3><div class="notice-list">${ann.length?ann.slice(0,5).map(a=>`<div class="notice-item"><strong>${esc(a.title)}</strong><p>${esc(a.project_name)} · ${esc(a.body)}</p>${a.requires_ack&&!a.acknowledged?`<div class="notice-actions"><button class="btn btn-secondary" data-ack="${a.id}" type="button">Acknowledge</button></div>`:''}</div>`).join(''):'<div class="today-empty">No active project announcements.</div>'}</div></div></div></div>`;document.querySelectorAll('[data-ack]').forEach(b=>b.onclick=async()=>{b.disabled=true;const {error}=await db.rpc('acknowledge_my_project_announcement',{p_announcement:b.dataset.ack});if(!error){b.textContent='Acknowledged';await loadHome()}else b.disabled=false});const badge=document.getElementById('todayUnreadBadge');if(badge){badge.textContent=x.unread_notifications||0;badge.style.display=x.unread_notifications?'inline-grid':'none'}}
async function loadHome(){const {data,error}=await db.rpc('get_my_contributor_home');if(error){document.getElementById('todayBody').innerHTML=`<div class="card empty">Unable to load contributor home: ${esc(error.message)}</div>`;return}home=data;renderToday()}
async function loadNotifications(){const body=document.getElementById('notificationsBody');if(!body)return;const {data,error}=await db.rpc('get_my_notifications',{p_limit:50});if(error){body.innerHTML=`<div class="today-empty">${esc(error.message)}</div>`;return}const items=data?.items||[];body.innerHTML=items.length?items.map(n=>`<div class="timeline-row" data-note="${n.id}"><span class="tl-dot" style="opacity:${n.read_at?0.4:1}"></span><div><strong>${esc(n.title)}</strong><div class="tl-meta">${esc(n.body)} · ${when(n.created_at)}</div></div>${n.read_at?'':'<button class="btn btn-secondary" data-read="'+n.id+'" type="button">Mark read</button>'}</div>`).join(''):'<div class="today-empty">No notifications yet.</div>';body.querySelectorAll('[data-read]').forEach(b=>b.onclick=async()=>{await db.rpc('mark_my_notification_read',{p_id:b.dataset.read});loadNotifications();loadHome()})}
async function loadSupport(){const body=document.getElementById('supportTickets');if(!body)return;const {data,error}=await db.rpc('get_my_support_tickets');if(error){body.innerHTML=`<div class="today-empty">${esc(error.message)}</div>`;return}body.innerHTML=(data||[]).length?(data||[]).map(t=>`<div class="timeline-row"><span class="tl-dot"></span><div><strong>${esc(t.ticket_code)} · ${esc(t.subject)}</strong><div class="tl-meta">${esc(t.category)} · ${esc(t.status)} · ${when(t.updated_at)}${t.admin_note?` · Team: ${esc(t.admin_note)}`:''}</div></div><span class="pill">${esc(t.priority||'normal')}</span></div>`).join(''):'<div class="today-empty">No support tickets yet.</div>'}
function wire(){document.getElementById('markAllNotifications').onclick=async()=>{await db.rpc('mark_all_my_notifications_read');loadNotifications();loadHome()};document.getElementById('supportForm').onsubmit=async e=>{e.preventDefault();const st=document.getElementById('supportStatus');st.textContent='Creating ticket…';st.className='notice';const {data,error}=await db.rpc('create_my_support_ticket',{p_category:document.getElementById('supportCategory').value,p_subject:document.getElementById('supportSubject').value,p_description:document.getElementById('supportDescription').value});if(error){st.textContent=error.message;st.className='notice error';return}st.textContent=`Ticket ${data.ticket_code} created.`;st.className='notice ok';e.target.reset();loadSupport();loadHome()}}
async function init(){style();addNav();addViews();wire();setupRouting();await loadHome();if(location.hash==='#notifications')loadNotifications();if(location.hash==='#support')loadSupport()}
window.addEventListener('DOMContentLoaded',()=>setTimeout(init,180));})();