(()=>{'use strict';
function wire(){const b=document.querySelector('[data-tab="tasks"]');if(!b)return;b.textContent='Quick Assignment';b.title='Search a participant and assign project tasks';b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();location.href='/admin/assignments'},true);const panel=document.getElementById('tasks');if(panel&&!panel.dataset.assignmentCenter){panel.dataset.assignmentCenter='1';const card=panel.querySelector('.card');if(card)card.innerHTML='<div class="section-title" style="margin:0 0 10px"><div><div class="eyebrow">Fast work distribution</div><h2>Quick Assignment</h2><div class="page-sub">Search a participant, choose a project, select L1 or L2, set the task target, and assign — all from one clean page.</div></div></div><a class="btn btn-primary" href="/admin/assignments">Open Quick Assignment →</a>'}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
})();