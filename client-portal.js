(()=>{
'use strict';
const U='https://llmhyezgcnbognmmsnzq.supabase.co',K='sb_publishable_QfaSTpmmj6reyY-kCsmhng_7PKvCGml',esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),fmt=v=>v?new Date(v).toLocaleString():'—';let db;
function pill(decision){return decision==='approved'?'pill-green':decision==='changes_requested'?'pill-red':'pill-gray'}
async function load(){const body=document.getElementById('portalBody');body.className='loading';body.textContent='Loading client access…';const {data:{user}}=await db.auth.getUser();if(!user){location.replace('/auth');return}const {data,error}=await db.rpc('get_my_client_portal');if(error){body.className='card empty-state';body.innerHTML=`<h2>Unable to load client access</h2><p class="page-sub">${esc(error.message)}</p>`;return}if(!data?.has_access){body.className='card empty-state';body.innerHTML='<h2>No client organization access</h2><p class="page-sub">This account is signed in, but it has not been added to a client organization yet. Ask an Adatacore Admin to link your account.</p><div style="margin-top:18px"><a class="btn btn-secondary" href="/workspace">Open workspace</a></div>';return}const orgs=data.organizations||[];body.className='org-list';body.innerHTML=orgs.map(o=>`<section class="card org-card"><div class="org-head"><div><div class="eyebrow">Client organization</div><h2 style="margin:5px 0 0">${esc(o.name)}</h2></div><span class="pill pill-brand">${esc(o.role)}</span></div><div class="project-list">${(o.projects||[]).length?(o.projects||[]).map(p=>`<article class="project-card"><div class="project-head"><div><h3 style="margin:0">${esc(p.name)}</h3><div class="page-sub">${esc(p.description||'Client-linked Adatacore project')}</div></div><span class="pill ${p.status==='active'?'pill-green':'pill-gray'}">${esc(p.status)}</span></div><div class="project-grid"><div class="metric"><span>Tasks</span><strong>${Number(p.tasks_total||0)}</strong></div><div class="metric"><span>Approved</span><strong>${Number(p.approved||0)}</strong></div><div class="metric"><span>Pending review</span><strong>${Number(p.pending_review||0)}</strong></div><div class="metric"><span>Deliveries</span><strong>${(p.deliveries||[]).length}</strong></div></div>${(p.deliveries||[]).length?`<div class="delivery-list">${p.deliveries.map(d=>`<div class="delivery-card"><div class="delivery-head"><div><strong>${esc(d.title)}</strong><div class="record-meta">Created ${fmt(d.created_at)} · Expires ${fmt(d.expires_at)}</div></div><span class="pill ${d.active?'pill-green':'pill-gray'}">${d.active?'Active':'Inactive'}</span></div><div class="delivery-meta"><span class="pill">${Number(d.items||0)} items</span><span class="pill pill-green">${Number(d.approved||0)} approved</span><span class="pill ${pill('changes_requested')}">${Number(d.changes_requested||0)} changes requested</span><span class="pill pill-gray">${Math.max(0,Number(d.items||0)-Number(d.approved||0)-Number(d.changes_requested||0))} remaining</span></div></div>`).join('')}</div>`:'<div class="portal-note">No formal review delivery has been created for this project yet.</div>'}</article>`).join(''):'<div class="card empty-state"><p class="page-sub">No projects are linked to this organization yet.</p></div>'}</div><div class="portal-note">Detailed audio/transcript review links remain individually secured. This portal provides persistent project and delivery visibility; a review link can still be shared for the underlying task collection.</div></section>`).join('')}
window.addEventListener('DOMContentLoaded',()=>{if(!window.supabase?.createClient)return;db=window.supabase.createClient(U,K,{auth:{persistSession:true,autoRefreshToken:true}});document.getElementById('refreshPortal').onclick=load;document.getElementById('signOut').onclick=async()=>{await db.auth.signOut();location.replace('/auth')};load()});
})();
