(()=>{'use strict';const U='https://llmhyezgcnbognmmsnzq.supabase.co',K='sb_publishable_QfaSTpmmj6reyY-kCsmhng_7PKvCGml';const db=supabase.createClient(U,K,{auth:{persistSession:true,autoRefreshToken:true}});function currentApplicationId(){return document.querySelector('.app-row.active')?.dataset?.id||''}async function removeApplication(){const id=currentApplicationId();if(!id){alert('Select an application first.');return}const ok=confirm('Delete this application?\n\nNo email will be sent to the participant. The application, its review history, and participant-facing application notifications will be removed. When they log in, it will appear as though they never applied for this application.\n\nThis action cannot be undone.');if(!ok)return;const btn=document.getElementById('deleteApplicationBtn');if(btn)btn.disabled=true;try{const {error}=await db.rpc('admin_delete_application',{p_application_id:id});if(error)throw error;location.reload()}catch(e){alert(e.message||'Unable to delete application.');if(btn)btn.disabled=false}}function inject(){const note=document.getElementById('reviewNote');if(!note)return;if(document.getElementById('deleteApplicationBtn'))return;const section=note.closest('.detail-section');if(!section)return;const wrap=document.createElement('div');wrap.style.cssText='margin-top:14px;padding-top:14px;border-top:1px solid var(--line)';wrap.innerHTML='<button type="button" id="deleteApplicationBtn" class="btn-mini" style="width:100%;background:rgba(225,29,72,.08);color:#be123c;border-color:rgba(225,29,72,.22)">Delete application</button><div style="margin-top:7px;font-size:9px;line-height:1.45;color:var(--muted)">Deletes the application without emailing the participant and resets it from their application view.</div>';section.appendChild(wrap);wrap.querySelector('#deleteApplicationBtn').onclick=removeApplication}const observer=new MutationObserver(inject);window.addEventListener('DOMContentLoaded',()=>{inject();const target=document.getElementById('detailBody')||document.body;observer.observe(target,{childList:true,subtree:true})})})();